generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model TestRecord {
  id    Int    @id @default(autoincrement())
  value String
}
